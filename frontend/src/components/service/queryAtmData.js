export const queryAtmData = async (page, dzongkhag) => {
   // let url = `http://localhost:8082/query_atm?page=${page}&dzongkhag=${dzongkhag}`;
  
   // const response = await fetch(url);
   // const data = await response.json();
   return [];
  };
  